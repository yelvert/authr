import React from 'react'
import ReactDOM from 'react-dom/client'

import ForwardAuthApp from '@app/forward_auth/app'

ReactDOM.createRoot(document.body).render(
  <React.StrictMode>
    <ForwardAuthApp />
  </React.StrictMode>
)
